import React from 'react';
import Modal from '../Common/Modal';

const BuyNowModal = (props) => {
    const { product, show, onClose } = props;

    return (
        <Modal show={show} onClose={onClose}>

        </Modal>
    )
}